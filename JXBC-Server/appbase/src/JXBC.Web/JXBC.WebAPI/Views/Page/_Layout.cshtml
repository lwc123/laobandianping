@using JXBC.WebCore;

<!DOCTYPE html>
<html lang="zh">
<head>
    <meta charset="utf-8">
    <title>@ViewBag.Title</title>
    <meta HTTP-EQUIV="pragma" CONTENT="no-cache">
    <meta HTTP-EQUIV="Cache-Control" CONTENT="no-cache, must-revalidate">
    <meta HTTP-EQUIV="expires" CONTENT="Wed, 26 Feb 1997 08:21:57 GMT">
    <meta name="viewport" content="width=device-width,height=device-height,initial-scale=1.0, minimum-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <link rel="shortcut icon" href="@(AppEnvironment.ResourcesSiteRoot)/juxianling.ico?v=0">
    @Html.Partial("_common_resources_header")
    @RenderSection("_resources_header", false)
</head>
<body @if (false == string.IsNullOrEmpty(ViewBag.BodyId)) { @Html.Raw("id=\"" + @ViewBag.BodyId + "\"") ; }
    @if (false == string.IsNullOrEmpty(ViewBag.BodyClass)) { @Html.Raw("class=\"" + @ViewBag.BodyClass + "\"") ; }>
    <!--<div>
        <p id="alertInfo" style="margin:30px;font-weight:bold;"></p>
    </div>-->

    @RenderBody()

    @Html.Partial("_common_resources_footer")
    @RenderSection("_resources_footer", false)
</body>
</html>